apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: auto-pe-l3-boundary-enforce

on:
  workflow_dispatch:

jobs:
  run:
    steps:
    - name: Run step
      uses: docker://alpine:3.18
      run: echo "scan-job running"